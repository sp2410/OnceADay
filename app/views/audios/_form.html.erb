<%= form_with(model: audio, local: true) do |form| %>
  <% if audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audio.errors.count, "error") %> prohibited this audio from being saved:</h2>

      <ul>
      <% audio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contenturl %>
    <%= form.text_field :contenturl, id: :audio_contenturl %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
